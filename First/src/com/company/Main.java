package com.company;

import java.util.Arrays;
import java.util.Scanner;
import java.util.Random;

/** Java-DOC comment */
public class Main {
    public static void main(String[] args) {
        /*Scanner input = new Scanner(System.in);  // для чтения ввода с консоли, System.in - стандартный поток ввода (обычно клавиатура)

        System.out.print("Input num1: ");
        float num1 = input.nextFloat();

        System.out.print("Input num2: ");
        float num2 = input.nextFloat();

        // когда nextLine() вызывается сразу после nextFloat(), он считывает оставшийся символ новой строки, а не ожидает ввода от пользователя
        input.nextLine();

        System.out.print("Input action: ");
        String action = input.nextLine();

        float res = switch (action) {
            case "+" -> num1 + num2;
            case "-" -> num1 - num2;
            case "*" -> num1 * num2;
            case "/" -> num2 != 0 ? num1 / num2 : -1;
            default -> -1;
        };
        if (res != -1) {
            System.out.println("The result is: " + res);
        }
        else {
            System.out.println("Invalid input...");
        }*/
        
    }
}